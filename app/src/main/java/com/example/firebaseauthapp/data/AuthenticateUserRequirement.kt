package com.example.firebaseauthapp.data

import com.example.firebaseauthapp.domain.models.AppRepository
import com.google.firebase.auth.PhoneAuthCredential

class AuthenticateUserRequirement {
    private val repository = AppRepository()

    //operator fun invoke(typedOtp: String) : Boolean = repository.authenticateUser(typedOtp)


}